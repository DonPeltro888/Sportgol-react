# SEO Module — README

⚠️ **Documentazione aggiornata**: vedi **`/app/SEO_PORTABLE_MODULE.md`** (versione 2.0, 2026-05-08).

Quel file contiene:
- Lista completa dei 41 file del modulo (backend + frontend)
- Setup step-by-step per progetti FastAPI + React + MongoDB (es. ticketgol.com)
- Tutte le rotte API (Generation pipeline + Intelligence Hub)
- Schema MongoDB richiesto
- Costi API per entity
- Schema.org coverage completo
- Checklist integrazione

## Quick start (per dev di ticketgol.com):
```bash
# Copia automatica completa del modulo SEO nel target project
/app/copy_seo_module.sh /path/to/ticketgol/repo
```

Lo script copia automaticamente tutti i 41 file e stampa i prossimi step.
